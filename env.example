# Copy this file to `.env` and customize values for local development.
# The Wikipedia REST API expects a descriptive User-Agent that includes
# a way to reach you (email or URL). Update the string below before use.
WIKI_USER_AGENT="SkiCycleRunWikiClient/1.0 (contact: you@example.com)"

# Google Maps Platform API key enabling Places Nearby lookups.
GOOGLE_MAPS_API_KEY="replace-with-your-google-api-key"
