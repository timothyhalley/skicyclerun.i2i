{
  "input_folder": "/Volumes/MySSD/ImageLib/phase1_extract/scaled",
  "output_folder": "/Volumes/MySSD/ImageLib/phase2_lora/processed",
  "input_image": "/Volumes/MySSD/ImageLib/phase1_extract/scaled/GymLady.jpeg",
  "output_format": "webp",
  "max_dim": 1024,
  "num_inference_steps": 24,
  "guidance_scale": 3.5,
  "device": "mps",
  "precision": "bfloat16",
  "preprocess": {
    "enabled": true,
    "cleanup": true,
    "face_detection": false
  },
  "cache_dir": "/Volumes/MySSD/huggingface",
  "_comment": "NOTE: style_name, prompt, negative_prompt, and lora are ONLY used when --lora is NOT specified. Since you always use --lora, those values come from config/lora_registry.json instead."
}
